---
title: "Installation"
sidebarTitle: "Installation"
description: "You'll learn how to install the ng-fast-toast library in your project."
publishDate: "2024-12-21"
author: {
    name: "rperezll",
    url: "https://github.com/rperezll"
}
---

## Prerequisites

To install and use this library, your project must meet the following **minimum requirements**:

- **Angular**: This library is compatible only with **Angular 18 or higher**.

<Alert type="info"> You have an example project with Angular 18, and ng-fast-toast installed for you to explore.</Alert>


If the listed prerequisites are met, we can proceed with the installation of the library.

## Install the library

To integrate toast notifications into your Angular project, you need to install the `ng-fast-toast` library. This package provides an *easy-to-use* service for displaying customizable toast notifications.

Run the following command to install it:

```bash
npm i ng-fast-toast@latest
```

<Alert type="info">If you want more information about the npm package, check out its [official npm page](https://www.npmjs.com/package/ng-fast-toast).</Alert>

## Usage

Now we're ready to start using ng-fast-toast. For that, let's head to the usage documentation:

<DocumentCard documentSlug="usage" />
